#include<iostream>
#include<string>
#include<map>
#include"solution.hpp"
using namespace std;
int main(){
  // string tmp_string;
  // cin>>tmp_string;
  // cout<<"there is input:"<<tmp_string<<endl;
  // map<char,int> map_p;
  // map<char,int> ::iterator it;
  //
  // map_p.insert(pair<char,int>('e',12));
  // // for(it=map_p.begin();it!=map_p.end();it++)
  // it = map_p.end();
  // it --;
  // cout<<it->first<<"    "<<it->second<<endl;
  Solution tmp;
  tmp.isMatch("1122333","");
  return 0;
}
